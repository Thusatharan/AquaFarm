import 'package:flutter/material.dart';

class Fish extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Fish'),
      ),
      body: Container(
        child: Text('This is Fish Page'),
      ),
    );
  }
}
